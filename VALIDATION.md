# TraceFox 0.0.1 发布验证

验证日期：2026-09-12。Windows x64，当前用户 EXE 安装包。

- `cargo test --locked`：57 项通过；1 项需真实样本副本的手动性能测量跳过。
- `cargo build --release --locked`：通过，程序文件版本与产品版本均为 0.0.1。
- `node scripts/test_storage_health.cjs`：通过。
- `scripts/validate_report.py`：搜索跨分页定位、磁盘行内详情关联、空结果、独立存储数据、两万行倒序证据、明暗主题及移动端无整页溢出验证通过，无浏览器脚本错误或外部网络请求。
- `scripts/validate_installer.py`：实际安装启动、还原后取消、还原后保存、重启、合成 TGZ 报告生成、升级保留规则、卸载保留规则均通过。
- `scripts/validate_webdav.py`：本地 HTTP 服务验证下载/同步保存路径一致、规则写入失败保留旧文件；设置测试不落盘、凭据回滚、重启、错误提示及 100%/150% 窗口验证通过。
- 发布前检查了全部历史文件中的诊断归档扩展名及常见凭据格式，未发现匹配项；这不等于穷尽所有敏感信息检测。

上述结果来自本地发布构建。实际公网 WebDAV 服务、其他 Windows 设备和代码签名未验证；正式 GitHub Actions 及附件状态在推送后单独核对。不将下面的历史样本结论当作本次复测结果。

---

## 历史记录（发布前开发版本）

### TraceFox 0.1.0 验证记录

验证日期：2026-09-09。交付方式：Windows x64 单 EXE，约 20.5 MB。

## 自动测试

17 项 Rust 测试通过，覆盖关键词组合、排除词、大小写、旧规则转换、规则往返导入、标识优先合并、上下文边界及重叠、gzip 轮转日志、容量格式化、取消保留旧报告、覆盖后隔离残留日志、时间合并、缺失时间、路径越界、链接条目、HTML 注入和 Linux 末尾点文件名。

## 六个真实样本

全部成功生成离线报告。以下为本机命令行分析实测；峰值内存通过 Windows 进程工作集采样取得，GUI 还包含界面开销，耗时与磁盘、规则及命中数量有关。

| 样本 | 分析耗时 | 峰值工作集 | HTML 大小 | 结果 |
|---|---:|---:|---:|---|
| 样本 1 | 0.92 秒 | 51.5 MB | 9.67 MB | 通过 |
| 样本 2 | 1.11 秒 | 17.0 MB | 0.70 MB | 通过 |
| 样本 3 | 3.22 秒 | 17.4 MB | 0.80 MB | 通过 |
| 样本 4 | 2.31 秒 | 18.2 MB | 1.81 MB | 通过 |
| 样本 5 | 2.62 秒 | 299.8 MB | 43.38 MB | 通过 |
| 样本 6 | 3.12 秒 | 47.0 MB | 14.09 MB | 通过 |

已对照原始 sysinfo.json 检查六个样本的磁盘数量、型号及序列号。内存总量通过 memInfo 规则提取；dmidecode 不提供条目时不伪造内存条信息。含 NUL 的日志会显示读取说明，原始文件不变。

## 浏览器与桌面

- 六份报告均通过 Edge 离线检查：系统信息子标签、时间线展开、前后匹配导航、全局搜索及跳转，无脚本错误、无网络资源请求。
- 主窗口在 100% 与 150% 显示缩放下检查，底部操作完整可见。
- 规则编辑保存和取消、导入预览及保存已实际验证。
- 真实 Windows 目录监控已验证：忽略已有包、等待新包稳定、同名覆盖重分析，以及窗口关闭后的托盘分析。
- 从 PATH 移除开发工具后，EXE 可独立启动。PE 导入依赖仅 Windows 系统 DLL，不依赖 Python、Rust 运行时、VC 运行时 DLL 或 WebView2。

## 验证范围

验证在当前 Windows 11 环境完成；尚未在独立的全新 Windows 10 虚拟机或物理机上验收。ZIP、安装器、自动更新和日志全文查看器不在首版范围内。

源码未提交或推送到远程仓库。用户提供的样本未包含在分发目录中。


## 分组与系统信息优化验证（2026-09-09）

本次在上述首版记录之后验证：

- `cargo test --locked`：21 项测试通过，新增整组排序、停用规则的分组顺序、参考配置只替换内置系统 ID，以及同型号/缺少序列号磁盘的 SMART 归属与预览回归。
- `cargo build --locked`：Windows 调试构建通过。本次未发布或更新分发目录。
- `scripts/validate_display.py`：Edge 实际加载离线报告，验证分组集中显示、隐藏无命中组、开关切回全部、聚合去重与高亮合并、复制/换行、搜索/导航、旧批次取消及 3,000 处匹配。通过正式分析入口核对存储层级、SMART、网络多地址/MTU和内存字段，无脚本错误。
- `scripts/validate_editor.py`：使用隔离的 LOCALAPPDATA，通过 Windows UI Automation 和鼠标拖动验证组内拖动/移动、整组移动、选择身份、复制删除、换组、搜索与折叠、保存重开和取消、参考配置预览及套用。正式用户配置不变。
- 本地参考报告内嵌数据生成的新报告已核对：5 块磁盘、每盘 SMART 行数、4 个网络接口均对应原始数据；没有 dmidecode 来源时显示缺失提示，不伪造内存条数据。原始参考报告未修改，真实数据仅用于本地验证。
- 已检查关键词、存储视图及规则编辑器截图；验证输出位于被 Git 忽略的 `target/display-validation`。

复现浏览器检查需安装 Playwright 并提供 Edge；桌面检查需 Windows、pywinauto 和 Pillow。两个脚本均使用 `target/debug/TraceFox.exe`，先完成调试构建。测试夹具使用虚构数据。


## 列表式规则编辑器验证（2026-09-09）

本次按确认的文字线框图改造关键词、系统信息和时间线编辑器，报告样式保留全局设置表单。没有升级配置格式、发布版本或覆盖 `dist` 分发程序。

- `cargo build --locked`：Windows 调试构建通过。
- `cargo test --locked`：24 项测试通过；新增规则插入/跨组移动及序列化重开、共享及隐藏成员的整组排序、系统信息移动后字段与关联身份保持测试。
- `scripts/validate_editor.py`：在隔离的 `LOCALAPPDATA` 下通过 Windows UI Automation 和真实鼠标验证弹窗屏蔽背景、完成/取消/Esc、三步新增及返回保留输入、复制与删除、预览不提交、组内/整组/跨组拖拽、折叠组落点、取消拖拽、关键词搜索、保存失败后重试与重开、三类规则新增、共享规则以及参考配置导入取消。
- 同一脚本实际打开 Windows 文件选择器加载虚构 JSON 样例，验证先手动输入来源字段、再从样例选择其他字段的回填与保存。字段输入使用双向绑定，避免手动输入后丢失后续模型更新。
- `scripts/validate_editor_layout.py`：使用 60 条虚构规则，在 100% 和 150% **应用缩放**下检查最小窗口、长名称省略、弹窗底部操作可见、Tab 不进入背景及拖动到边缘自动滚动；未修改 Windows 全局缩放设置。
- `scripts/validate_display.py`：既有离线报告回归通过，覆盖分组、去重/高亮、搜索导航、3,000 处匹配、存储身份/SMART、网络和内存展示，无浏览器脚本错误。

截图位于 `target/display-validation`：`editor-overview.png`、`editor-keyword-config.png`、`editor-new-source.png`、`editor-new-content.png` 为实际应用中的示例规则截图；`editor-system-fields.png` 与 `editor-minimum-100.png` / `editor-minimum-150.png` 等为验证截图。用户正式配置和真实诊断文件未参与这些测试。

复现需先构建调试程序，再依次执行两个编辑器脚本，避免鼠标操作互相干扰。依赖沿用 `validation/python_deps` 中的 pywinauto、Pillow 和 Playwright；报告脚本可用以下方式加载本地依赖：

```powershell
python -c "import sys,runpy; sys.path.insert(0,'validation/python_deps'); runpy.run_path('scripts/validate_display.py',run_name='__main__')"
```

## 报告批量展示与快速收起验证（2026-09-10）

- `cargo test --locked`：19 项测试通过；新增报告行数默认值、旧配置兼容及范围校验。
- `scripts/validate_display.py`：Edge 离线加载虚构报告，验证关键词直接聚合、旧导航控件移除、按批显示 3,000 条匹配、继续显示、粘性收起、焦点回到摘要、跨批搜索定位、移动端 44px 触控高度、粘性操作栏样式及减少动态效果下的即时收起，无脚本错误。
- `scripts/validate_editor.py`：在隔离的 LOCALAPPDATA 下验证“日志每批显示”设置保存、重新打开后继续修改，以及原有编辑器流程不受影响。
- `cargo fmt --check` 与 `git diff --check`：通过。报告模板仍保留完整内嵌数据，行数配置只限制浏览器当前渲染批次。

## 解压与分析性能优化（2026-09-10）

本次从修改前的当前源码重新构建 Release 后对比，未使用旧 `dist` 程序作为对照。真实诊断包约 81.6 MiB，仅处理 `target/performance` 内的副本，使用相同内置规则，各模式独立运行三次，以下为中位数。首次解压指输出目录不存在，并非清空操作系统文件缓存。

| 指标 | 修改前 | 修改后 |
|---|---:|---:|
| 首次处理完整链路 | 2.676 秒 | 2.513 秒 |
| 覆盖处理完整链路 | 2.643 秒 | 2.467 秒 |
| 首次完整性校验 | 0.391 秒 | 0.341 秒 |
| 首次解压及规则准备 | 0.838 秒 | 0.750 秒 |
| 首次分析 | 1.427 秒 | 1.379 秒 |
| 首次报告写入 | 0.033 秒 | 0.032 秒 |
| 首次处理峰值工作集 | 78.56 MiB | 29.50 MiB |
| 桌面实际导入／重试，提交至报告生成 | 2.820 秒 | 2.589 秒 |

完整链路通过忽略式性能测试调用与桌面相同的 `validate_archive → analyze`，包含前置校验。解压阶段时间包含首个来源开始分析前的规则准备，分析阶段包含结果归并；不是 CPU 采样结果。内存为测试进程工作集，不含 GUI。另以隔离配置启动两个 Release EXE，通过 Windows 文件对话框导入并重试两次，核对成功状态和报告；桌面采样峰值工作集中位数为 131.52 → 111.55 MiB。监控稳定等待和任务排队策略未变。

第一轮仅做缓冲、复用和流式输出时，真实样本总耗时没有明显下降；最终启用现有 flate2 的纯 Rust `zlib-rs` 后端，完整链路约缩短 6–7%，桌面约缩短 8%。未新增依赖版本或配置项，PE 导入检查未发现额外解压运行时 DLL。

专项虚构样例包含 2,000 条自定义时间表达式命中，以及三条规则各提取 2,000 个带正则捕获的 JSON 字段。相同规则、交替运行修改前后 EXE 各三次，命令行耗时中位数为 1.132 → 0.045 秒；六次完整报告数据一致。此收益针对重复正则编译场景，不代表所有诊断包的提速比例。

- `cargo test --locked`：31 项通过；性能测量入口默认忽略，不设置耗时门禁。新增紧凑 JSON 回退与自定义时间证据、共享来源的独立错误、流式转义与写入失败、报告生成时取消／源包变化、gzip CRC 损坏回归。
- `cargo build --release --locked`、`cargo fmt --check`、`git diff --check`：通过。
- `scripts/validate_display.py`：Edge 离线回归通过，覆盖分组、上下文去重与高亮、搜索导航、3,000 条匹配分批渲染、存储／SMART、网络和内存，无浏览器脚本错误。
- 真实样本修改前后各六次完整链路输出，以及两个桌面版本最终输出，除 `generated` 外逐项比较完整报告数据一致；不只比较命中数。
- 产物为 `target/release/TraceFox.exe`；原始数据和正式配置未修改，未覆盖 `dist`，未提交、推送或发布。

复现阶段测量：先运行 `cargo test --release --locked --test performance --no-run`，将输出中的测试 EXE 路径传给 `python scripts/measure_performance.py <测试EXE> <诊断包> <新的隔离输出目录>`。脚本自行复制诊断包，每次先首次解压再覆盖解压，保存分阶段数据和移除生成时间的完整报告 JSON。需要 `psutil`，可沿用 `validation/python_deps`。本次记录、桌面测量脚本与截图位于被 Git 忽略的 `target/performance`。


## 桌面窗口紧凑化（2026-09-10）

- 主窗口默认 560×460、最小 520×420；规则编辑器默认 880×680、最小 800×580；导入预览默认 560×420、最小 520×360（均为应用逻辑尺寸）。统一内层布局留白，固定操作行伸缩，报告行固定 48px；滚动内容按实际高度排列，防止单条报告填满列表。
- `cargo build --locked`、`cargo test --locked`：通过，37 项测试成功，1 项性能入口按默认忽略。仍存在既有 WebDAV 同步分支的 unreachable pattern 警告。
- `scripts/validate_editor.py`：本轮完整通过新增/返回、保存取消、样例预览、跨组拖动、导入和配置重开；调整滚轮定位，先移到表单空白区域再滚动。
- `scripts/validate_editor_layout.py`：将验收尺寸更新为 800×580，在 100%/150% 应用缩放下通过长名称、模态焦点、底部按钮和边缘自动滚动检查。
- `scripts/validate_webdav.py`：通过独立弹窗、各页入口、测试不落盘、保存回滚、密码保留、失败提示、焦点/Esc 和缩放检查。
- `scripts/validate_compact.py`：在隔离配置下实际导入虚构 TGZ，主窗口、报告行、编辑器、WebDAV、导入预览在 100%/150% 应用缩放下通过；截图位于 `target/compact-validation`。应用缩放不等同于更改 Windows 全局 DPI。
- 最后一轮重复编辑器验证遇到 `screen grab failed`，并在 Esc 焦点断言处中断，不能将该轮重复运行记为通过。此前完整运行通过；报告行最终修复后的两档缩放导入检查通过。
- `cargo fmt --check` 未通过：既有 Rust/WebDAV 改动存在格式差异，本次界面优化未重排这些无关文件。`git diff --check` 通过。
- 本次未修改业务逻辑、报告模板或用户正式配置；未提交、推送、发布或覆盖 dist。


## 统一分析列表与监控清理（2026-09-10）

- 主窗口保持 560×460、任务行 48px。等待、检查、解压、分析、取消、失败和成功都归属同一任务行；同一路径重试原位更新。后台以稳定任务 ID 回传，列表原位更新以保留菜单、焦点和滚动。
- 监控改为按钮式“开启监控／暂停监控”，不是勾选框或滑块；目录、提示和“清理已完成”集中显示。内部扩展路径前缀仅在显示时隐藏。
- 损坏或不完整的 TGZ 行内仅显示“删除”；确认后删除源包和记录，保留旧报告目录。其他分析失败、取消仍可重试。批量清理只处理当前监控目录、本次成功且仍在列表中的包和输出，活动任务期间不可用；确认后复核版本，异步逐项执行，失败保留记录。
- `cargo test --locked`：45 项通过，性能入口 1 项默认忽略。覆盖稳定身份、同名不同目录、终态上限、取消及重试、旧报告、清理目录筛选、暂停调度、文件变化跳过、部分删除失败重试、损坏包删除及旧目录保留。
- `scripts/validate_tasks.py`：100%/150% 应用缩放均通过实际监控新增、等待取消、重试、更多菜单持续打开、原行重新分析、损坏包只删除、批量清理拒绝/确认、范围隔离、损坏包删除确认及旧报告保留、重新下载后处理。使用临时目录，正式用户文件未参与。截图位于 `target/task-validation`。
- 初次按钮键盘验证中曾发生监控意外关闭，后续带阶段状态检查的完整两档运行通过；未将首次失败记作成功。
- `cargo build --locked`、`git diff --check` 通过。`cargo fmt --check` 仍受既有 Rust/WebDAV 格式差异影响；既有同步分支 unreachable pattern 警告保留。本次新增任务模块和修改的桌面主流程已格式化。
- 不更改 Windows 全局 DPI；应用缩放不等同于真实系统 DPI 验收。未提交、推送、发布或覆盖 dist。

- 最终显示修正后，`scripts/validate_compact.py` 两档缩放通过主窗口、编辑器入口、导入预览、WebDAV 弹窗、实际手动导入及放大窗口后按钮高度检查。
